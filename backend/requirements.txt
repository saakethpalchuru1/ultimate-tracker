httpx==0.27.2
beautifulsoup4==4.12.3
# Optional -- only needed if you want to run the local FastAPI dev server.
# The Render cron path uses scripts/update_snapshots.py and doesn't need
# either of these.
fastapi==0.115.0
uvicorn[standard]==0.32.0
apscheduler==3.10.4
# Dev only:
pytest==8.3.3
